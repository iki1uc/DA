export async function DA_BOOT() {

    const pre = await DA_PREFETCH("DA");

    return {
        axiome: DA_AXIOM,
        prefetch: pre,
        vector: "NC.vector.next-ready",
        state: "DA-BOOT-OK"
    };
}
